# frozen_string_literal: true

RSpec.describe RailsI18nExtension do
  it "has a version number" do
    expect(RailsI18nExtension::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
